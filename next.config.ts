import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Removendo configuração de CORS que estava interferindo com API externa
};

export default nextConfig;
